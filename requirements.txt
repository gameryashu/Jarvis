# JARVIS — Python Dependencies
# Install with: pip install -r requirements.txt
# Some packages require system-level dependencies (see README).

# ── Core ──────────────────────────────────────────────────
anthropic>=0.25.0          # LLM (Claude) — primary
openai>=1.0.0              # LLM (GPT-4) — alternative
ollama>=0.1.0              # Local LLM via Ollama — alternative

# ── Speech-to-Text ────────────────────────────────────────
openai-whisper>=20231117   # Local STT (requires ffmpeg)
SpeechRecognition>=3.10.0  # Google STT alternative
pyaudio>=0.2.13            # Microphone capture (requires portaudio)
# deepgram-sdk>=3.0.0      # Deepgram STT — optional

# ── Text-to-Speech ────────────────────────────────────────
pyttsx3>=2.90              # Local TTS (offline)
gTTS>=2.3.0                # Google TTS (online)
playsound>=1.3.0           # Audio playback
# elevenlabs                # ElevenLabs TTS — optional

# ── Computer Control ──────────────────────────────────────
pyautogui>=0.9.54          # Mouse and keyboard automation
pyperclip>=1.8.2           # Clipboard access
Pillow>=10.0.0             # Screenshot processing
pytesseract>=0.3.10        # OCR (requires tesseract-ocr system package)
plyer>=2.1.0               # Desktop notifications

# ── HTTP / Async ──────────────────────────────────────────
aiohttp>=3.9.0             # Async HTTP (dashboard server)
httpx>=0.25.0              # Async HTTP client (ElevenLabs etc.)

# ── Memory / Embeddings ───────────────────────────────────
sentence-transformers>=2.2.0  # Semantic search (optional, heavy)
numpy>=1.24.0
PyYAML>=6.0.1

# ── Utilities ─────────────────────────────────────────────
python-dotenv>=1.0.0       # .env file support
rich>=13.0.0               # Better terminal output
